type ConsumeMatchFunction = (codePoint: number) => boolean;
// eslint-disable-next-line @typescript-eslint/consistent-type-imports
type ParseFunction = (parser: import('./parser').Parser) => boolean;
